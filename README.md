# Patoq - React Native App

یک اپلیکیشن React Native ساخته شده با Expo که آماده برای توسعه است.

## ✨ ویژگی‌ها

- ✅ Safe Area تنظیم شده
- ✅ Pull to Refresh فعال
- ✅ Navigation با Expo Router
- ✅ پشتیبانی از Dark/Light Mode
- ✅ TypeScript
- ✅ ساختار منظم فولدرها
- ✅ کامپوننت‌های پایه آماده

## 📁 ساختار پروژه

```
Patoq/
├── app/                    # صفحات اصلی (Expo Router)
│   ├── (tabs)/
│   │   ├── index.tsx      # صفحه خانه
│   │   ├── explore.tsx    # صفحه کاوش
│   │   └── _layout.tsx    # Layout تب‌ها
│   ├── modal.tsx          # صفحه مودال
│   └── _layout.tsx        # Layout اصلی
├── src/                   # کد اصلی پروژه
│   ├── components/        # کامپوننت‌ها
│   │   ├── common/       # کامپوننت‌های مشترک
│   │   └── ui/           # کامپوننت‌های UI
│   ├── screens/          # صفحات اضافی
│   ├── services/         # سرویس‌های API
│   ├── hooks/            # Custom Hooks
│   ├── utils/            # توابع کمکی
│   ├── types/            # تعریف Type ها
│   └── constants/        # ثابت‌های پروژه
├── components/           # کامپوننت‌های Expo (themed)
└── assets/              # تصاویر و فایل‌ها
```

## 🚀 نصب و راه‌اندازی

1. نصب dependencies:
   ```bash
   npm install
   ```

2. اجرای اپلیکیشن:
   ```bash
   npx expo start
   ```

## 🛠️ کامپوننت‌های آماده

### کامپوننت‌های مشترک
- `Button` - دکمه با انواع مختلف
- `LoadingSpinner` - نمایش لودینگ

### Hook های آماده
- `useApi` - مدیریت درخواست‌های API

### سرویس‌ها
- `apiService` - سرویس API با متدهای GET/POST

## 📝 نحوه استفاده

### استفاده از کامپوننت‌ها:
```tsx
import { Button, LoadingSpinner } from '@/src/components';

// استفاده از دکمه
<Button 
  title="کلیک کن" 
  onPress={() => console.log('clicked')}
  variant="primary"
/>

// استفاده از لودینگ
<LoadingSpinner text="در حال بارگذاری..." />
```

### استفاده از API:
```tsx
import { useApi } from '@/src/hooks/useApi';
import { apiService } from '@/src/services/api';

const { data, loading, error } = useApi(() => 
  apiService.get('/users')
);
```

## 🎨 تنظیمات

- رنگ‌ها و اندازه‌ها در `src/constants/index.ts`
- Type ها در `src/types/index.ts`
- توابع کمکی در `src/utils/index.ts`

## آماده برای توسعه! 🚀